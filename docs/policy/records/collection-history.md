# 정책 수집 실행 이력

이 문서는 날짜별 관찰·분석 근거를 보존한 작업용 기록이다. 당시의 완료·미구현·진행 중 표현, 건수와 명령을 현재 상태나 실행 지시로 사용하지 않는다. 현재 작업은 [작업 문서 안내](../work/README.md)를 따른다. 원문·fixture는 수정하지 않았다.

마지막 보존 기록은 2026-09-09의 `PARTIAL / USER_STOPPED`이며 직전 저장 오류와 종료·한도 복구 이력을 포함한다. 당시 전체 보관 4,632건은 현재 실시간 건수가 아니다. 별도 수집 요청 전까지 재개하지 않는다.

## 기록 찾아보기

- [전체 범위 수집 1차 결과](#policy-expansion-20260908)
- [초기 정책 후보 수집 재개](#policy-bulk-20260908)
- [2026-09-09 정책 수집 실행 결과](#policy-collection-20260909)

<a id="policy-expansion-20260908"></a>

## 전체 범위 수집 1차 결과

2026-09-08 개발 DB 확인. 기존 50개에서 **215개**로 증가했다. 출처별 레코드이며 출처 간 고유 정책 수는 아니다.

설정은 [automatic-all.json](../../../scripts/policy/automatic-all.json): 검색 제한 없음, 페이지 크기 50, 최대 1000페이지, 출처별 UTC 일일 예약 호출 상한 100회. 실제 공공데이터 계정 할당량을 측정한 값은 아니다.

| 출처 | 현재 저장 | 이번 실행 처리 성공 | API 응답 전체 건수 | 이번 실행 호출 |
| --- | ---: | ---: | ---: | ---: |
| 복지로 중앙 | 85 | 49 | 461 | 50 |
| 복지로 지자체 | 99 | 97 | 4,763 | 100 |
| Gov24 | 31 | 23 | 10,956 | 100 |

이번 실행에서 169개를 처리했고 중앙의 기존 4개는 갱신했다. 순증은 165개다. 중앙은 앞선 양육 검색에 사용한 50회를 합쳐 오늘 예약량이 100회가 됐다. 다른 출처도 각각 100회에 도달했다. 응답 전체 건수는 목록 탐색 중에 관측한 값이며 전체 수집 완료·중복 제거·육아 정책 분류를 뜻하지 않는다.

<a id="policy-expansion-20260908--품질과-실행-종료"></a>

### 품질과 실행 종료

- 현재 정책 215개 모두 품질 평가 대응. REVIEW 215, ERROR 0, 현재 미평가 0.
- 필수 6개 항목 존재 합계 1,290/1,290. 내용 정확도나 자격 비교 준비 완료가 아니다. 비교 준비 완료 0.
- 모든 실행은 CALL_BUDGET으로 일시정지(실행 테이블 PARTIAL). 전체 목록 수집은 아직 미완료다. 잠금 해제 확인.
- 지자체 첫 50개 뒤 목록 2페이지 오류가 있었지만 한 차례 재개하여 정상 응답(50개, 고유 ID 50개)을 확인하고 나머지 예산까지 처리했다.
- Gov24는 15개 저장 후 저장 경로 오류로 프로세스가 종료됐다. 최초 원인은 확정하지 못했다. 잠금 만료 후 같은 실행에서 재개해 8개를 추가 처리했다. 재개 시 같은 저장 오류는 재현되지 않았다.
- 민감한 요청 정보가 포함될 수 있는 진단 응답은 `.local/policy-sync/automatic`에만 보관한다. 관리자용 최종 품질 보고서는 같은 경로의 `expanded-quality-report.json`이다.

<a id="policy-expansion-20260908--다음-재개"></a>

### 다음 재개

일일 예약일은 UTC 00:00(KST 09:00)에 바뀐다. 이번 실행 시점 기준 다음 예약일은 2026-09-09 KST 09:00이다. 예약 실행기는 연결되지 않았으므로 자동으로 다시 실행되지는 않는다.

```bash
POLICY_SYNC_ENABLED=true npm run policy:auto -- --provider central --resume 87f0678c-b4f1-4bb4-a9bf-2153cf37ba57 --call-budget 100
POLICY_SYNC_ENABLED=true npm run policy:auto -- --provider local --resume 547a59cf-f9b3-4d86-be97-318be3ef4455 --call-budget 100
POLICY_SYNC_ENABLED=true npm run policy:auto -- --provider gov24 --resume 8a75d68a-9a14-4ae0-ac8a-02568b5902fd --call-budget 100
```

관리자 정책 수집 화면에서도 재개할 수 있다. 이번에 CLI 설정(페이지 크기 50·검색 범위 등)을 웹 재개에서 기본값으로 바꾸던 문제를 수정했다. 저장된 설정을 조회해 그대로 사용하고 웹 배치 예산은 기존 10회/5개로 유지한다. 재개 회귀 검사와 기존 접근 차단 검사, 타입·린트·webpack 빌드 통과.

<a id="policy-bulk-20260908"></a>

## 초기 정책 후보 수집 재개

**v3 재평가 후 재개:** 전체 2,277건의 v3 재평가와 관리자 집계 검증을 완료했다(활성 842·검토 845·제외 590). 원본 스냅샷 2,482건 보존·평가 이력 5,809건·현재 입력 연결 오류 0. 아동 본인 지원을 포함한 새 기준으로 `bulk-child-v3-target-1000.mjs`를 실행해 정부24 수집을 이어간다. 목표 ACTIVE 1,000건과 제한·오류 출처 제외 조건은 동일하다. 현재 진행·최종 상태는 `bulk-child-v3-target-1000.log`·`bulk-child-v3-target-1000-journal.json`이 기준이다. 아래는 이전 실행별 기록이다.

**아동 정책 재검사로 일시 중단:** 18:10 KST 현재 실행을 정상 종료했다. 활성 734·검토 952·제외 591(전체 2,277)이며 잠금 0과 내부 상한 전부100 복구를 확인했다. 검토 대기 중 아동·청소년 본인 대상 정책 누락을 v3로 보완한 뒤 재평가하고, 활성 1,000개 목표에 미달하면 정부24를 이어간다. 기존 제한·오류 출처 제외는 유지한다.

**최신 목표:** 사용자 승인에 따라 활성화된 정책 1,000건을 목표로 한다. 기존 v2 실행은 17:58 KST 정상 종료·설정 복구를 확인한 뒤 `bulk-target-1000.mjs`로 정부24를 재개한다. 매 배치 전 실제 ACTIVE 건수를 확인하고 남은 목표 이하로 처리 항목 수를 제한한다. TARGET_REACHED 또는 이용 가능한 출처의 종료·오류·한도 도달 시 중단하고 원래 상한을 복구한다. 복지로 중앙(일일 한도22)·지자체(UPSTREAM_ERROR)는 이번 실행에서 제외하며 재시도하지 않는다. 진행·종료 증거는 `bulk-target-1000.log`와 `bulk-target-1000-journal.json`에 남긴다.

**v2 기준 추가 수집:** 2026-09-08 17:52 KST 사용자 지시에 따라 정부24·복지로 지자체를 기존 진행 위치에서 재개했다. 복지로 중앙은 일일 한도 응답 코드22가 확인되어 이번 실행에서 제외했다. 실행은 `.local/policy-sync/automatic/bulk-v2.mjs`, 기록은 `bulk-v2.log`·`bulk-v2-journal.json`이다. 각 출처는 제공기관 오류·한도 또는 전체 완료 시 종료하고 임시 내부 상한을 복구한다. 시작 시 전체 1,247건(활성 304·검토 352·제외 591), 신규 수집 제외 이력 29건이다. 복지로 지자체는 17:52:48 KST에 67회 호출·95개 항목 처리 후 UPSTREAM_ERROR로 종료했고 상한 복구를 확인했다. 정부24는 17:55 KST에 19페이지 진행 위치(원천 전체 10,956개)로 계속 수집 중이다. 전체 완료 기록은 아직 없으며 최종 여부는 저널의 `finishedAt`과 출처별 `restored`를 확인한다.

**후속 중단:** 2026-09-08 17:24 KST 사용자의 제외 기준 강화 요청에 따라 두 연속 실행을 정상 종료했다. 최종 보관 1,247건(기존 기준 활성 268·검토 964·제외 15), 신규 수집 제외 29건. 원본 스냅샷 1,266건·관련성 이력 1,252건을 보존했으며, 모든 잠금 해제·출처별 내부 상한100 복구를 확인했다. 후속 v2 재평가를 완료해 현재 활성 304·검토 352·제외 591건이며, 원본 스냅샷 1,266건은 유지하고 관련성 이력은 2,499건으로 증가했다. [강화 기준과 검증](../work/classification.md). 아래는 중단 전 진행 관측 기록이다.


사용자 지시: 현재는 서비스 관련 정책과 관련 가능성이 있는 정책을 최대한 확보한다. 소득·연령·신청 자격 조건 해석은 추후 진행한다. 검토 필요도 보관하며 명확한 무관 정책만 신규 저장에서 제외한다.

2026-09-08 17:22 KST 관측값(진행 중):

| 구분 | 건수 |
| --- | ---: |
| 전체 보관 | 837 |
| 활성 후보 | 190 |
| 검토 대기 | 632 |
| 기존 제외 보관 | 15 |
| 이번 신규 저장 제외 | 10 |

출처별 보관은 중앙 88, 지자체 296, 정부24 453건이다. 출처 간 동일 정책 통합 건수나 최종 수집 완료 건수가 아니다. 재개 전 전체 보관은 215건이었다.

- 정부24는 원래 전체 범위 실행 `8a75d68a-9a14-4ae0-ac8a-02568b5902fd`를 연속 처리 중이다.
- 복지로 지자체는 원래 실행 `547a59cf-f9b3-4d86-be97-318be3ef4455`가 일시 중단된 뒤 한 차례 재개하여 100회 배치를 정상 처리하고 계속 진행 중이다.
- 복지로 중앙 `87f0678c-b4f1-4bb4-a9bf-2153cf37ba57`은 재확인 요청에서 외부 HTTP429·응답 코드22를 확인하여 추가 재시도하지 않았다. 내부 상한은100으로 복구했고 진행 위치를 보존했다. 외부 제한 해제 시점은 확인되지 않았다.

이번 실행에만 해당 작업과 일일 사용량의 내부 상한을100000으로 올렸다. 100회 호출 배치를 연속 처리하므로 완료·오류·실제 일일 상한에 도달할 때까지 이어간다. 외부 제공기관 제한을 무시하지 않으며 호출 사용량을 초기화하지 않는다. 각 출처가 종료·중단하면 원래100 상한으로 복구한다. 운영 예약은 켜지 않았다.

운영 기록은 Git에서 제외된 다음 파일에 남는다.

- `.local/policy-sync/automatic/bulk-once.log`, `bulk-once-journal.json`: 최초 세 출처 실행. 정부24 진행 중.
- `.local/policy-sync/automatic/bulk-retry.log`, `bulk-retry-journal.json`: 중단된 복지로 두 출처의 한 차례 진단 재개. 지자체 진행 중.
- 실행 스크립트 `bulk-once.mjs`, `bulk-retry.mjs`는 SIGTERM/SIGINT를 받으면 현재 배치 뒤 종료하고 설정을 복구한다. 강제 종료 등으로 복구가 누락되면 해당 스크립트의 `--restore`로 저널에 기록된 설정을 복구할 수 있다. 실행 중에는 복구 명령을 동시에 호출하지 않는다.

저널의 `finishedAt` 및 모든 해당 출처의 `restored`와 로그의 `bulk-finished`를 확인해야 전체 실행 종료다. 중간 배치 CALL_BUDGET은 전체 작업 종료를 의미하지 않는다. 실제 정책·분류·제외 건수는 관리자 정책 관리와 정책 수집에서 확인한다.

<a id="policy-collection-20260909"></a>

## 2026-09-09 정책 수집 실행 결과

대상: `policy-dev` · 실제 정부24·복지로 API 조회와 개발 DB 저장 · 1차 조회: 2026-09-09 17:50:58 KST

사용자의 오늘 정책 수집 요청에 따라 기존 수집기를 실행했다. **1차 실행은 종료됐지만 전체 출처 수집은 미완료(`PARTIAL`)**다. 정부24·복지로 중앙은 기존 내부 일일 예약 상한에 도달했고, 복지로 지자체는 다음 목록 조회 오류로 중단했다. 모든 프로세스 종료와 관측된 수집 잠금 해제를 확인했다.

<a id="policy-collection-20260909--결과"></a>

### 결과

| 출처 | 정제 반영 | 신규 저장 제외 | 항목 실패 | 오늘 예약 호출 | 종료 사유 |
| --- | ---: | ---: | ---: | ---: | --- |
| 정부24 | 24 | 0 | 0 | 100 / 100 | `DAILY_BUDGET` |
| 복지로 중앙 | 91 | 17 | 0 | 100 / 100 | `DAILY_BUDGET` |
| 복지로 지자체 | 49 | 1 | 0 | 52 / 100 | `UPSTREAM_ERROR` |
| 합계 | 164 | 18 | 0 | 252 | 전체 범위 미완료 |

전체 보관 레코드는 **2,527 → 2,568건, 순증 41건**이다. 반영 164건에는 기존 정책 재수집이 포함되며 전부 신규 정책이라는 뜻이 아니다. 출처 간 동일 정책을 통합한 고유 정책 수·공개 정책 수·수혜 가능 정책 수도 아니다.

이번 반영의 품질 기록 164건은 모두 `REVIEW`이고 기술적 `ERROR` 행은 0건이다. 항목 실패 0건과 별개로 지자체 목록 조회는 오류가 있었다. 최종 전체 현재 표시값의 품질 미평가 수는 0건이며, 품질 검사는 원문 의미·공식성·자격·공개 승인을 대신하지 않는다.

<a id="policy-collection-20260909--기존-실행-재개와-오늘-실행"></a>

### 기존 실행 재개와 오늘 실행

먼저 세 출처의 기존 미완료 실행을 재개했으나 각각 1회 목록 확인 후 `SOURCE_DRIFT`로 중단됐다. 어제 저장된 목록과 오늘 응답이 달라 같은 실행에 섞지 않고, 기존 원본·진행 기록을 보존한 채 새 실행을 시작했다. 예약 호출 표에는 이 확인 비용도 포함된다.

오늘 설정은 출처 전체(`filters: {}`), 페이지 크기 50, 페이지 상한 1,000, 출처별 일일 예약 상한 100, 프로세스 호출 예산 100이다. 임시 상한 확대나 사용량 초기화는 하지 않았다. 일일 예약은 UTC 날짜 기준이며 공공데이터포털의 실제 계정 할당량과 동일한 값이라고 보지 않는다.

| 출처 | 오늘 실행 ID | 새 실행 호출 | 발견 / 원천 목록 총수 | 발견 항목 중 미완료 |
| --- | --- | ---: | ---: | ---: |
| 정부24 | `8ef95422-a4d3-4474-ba3e-9cbd186899e3` | 99 | 50 / 10,955 | 26 |
| 복지로 중앙 | `851043be-6342-4dff-b91b-f03424d9d266` | 99 | 150 / 461 | 42 |
| 복지로 지자체 | `78f41e01-e944-459d-a025-d60ecd441b5a` | 51 | 50 / 4,764 | 0 |

‘발견 항목 중 미완료 0’은 전체 출처 완료가 아니다. 지자체는 1페이지 항목을 처리한 뒤 2페이지 조회에서 멈췄다. 세 실행 모두 `discovery_complete=false`다. CLI는 `PAUSED`와 exit 0으로 종료했지만 DB 상태는 `PARTIAL`이므로 성공 종료로 보고하지 않는다.

<a id="policy-collection-20260909--다음-재개-기준"></a>

### 다음 재개 기준

- 정부24·중앙: 예약일과 실제 계정 한도 확인 후 **오늘 새 실행 ID**를 재개한다. 예전 실행의 목록을 오늘 목록에 섞지 않는다.
- 지자체: 제공기관 응답·목록 검증 실패 원인을 확인한 뒤 재개한다. 이번 작업에서 기록한 오류는 `UPSTREAM_ERROR`이며 정확한 HTTP/응답 코드까지 확인한 것은 아니다. 동일 요청을 반복하거나 한도를 올려 재시도하지 않았다.
- 재개 시에도 목록 변경이 감지되면 [운영 계약](../work/collection-runbook.md)에 따라 새 실행 여부를 판단한다. 남은 정책을 모두 수집했다고 해석하지 않는다.
- 수집 시 기존 관련성 평가·자동 라벨 제안 트리거가 적용될 수 있다. 이번 작업은 인간 검수·추천 공개 승인·추천 알고리즘 적용이 아니다.

<a id="policy-collection-20260909--실행-증거"></a>

### 실행 증거

- 저장소 요약: [실행·검증 JSON](../../fixtures/policy-automatic/collection-20260909.json).
- 로컬 상세: `.local/policy-sync/daily-20260909/before.json`, `after.json`, 출처별 `*-resume.log`, `*-fresh.log`, `config.json`.
- 수집기 보고서: `.local/policy-sync/automatic/<실행 ID>.json`.

저장소 JSON에는 실행 ID·설정·실제 호출 및 반영 수·상태·상세 파일 해시를 남겼다. 로컬 상세 파일은 Git 제외 자료이며 다른 환경에서 자동으로 존재하지 않는다. 원격 스키마 변경·배포·예약 실행은 수행하지 않았다.


<a id="policy-collection-20260909--임시-한도-확대-후-추가-수집--1817-kst-확인"></a>

### 임시 한도 확대 후 추가 수집 · 18:17 KST 확인

이후 사용자가 이번 실행에 한해 내부 한도를 우회하도록 요청했다. 일일 사용량을 초기화하지 않고 설정만 100 → 100,000으로 임시 확대했다. 아래 수치는 1차 실행과 같은 실행 ID의 누적 결과다.

| 출처 | 정제 반영 | 무관 제외 | 실패 | 오늘 예약 호출 | 중단 사유 |
| --- | ---: | ---: | ---: | ---: | --- |
| 정부24 | 866 | 163 | 0 | 3,900 | 미저장 우선 전환 요청에 따라 배치 종료 후 정지 |
| 복지로 중앙 | 94 | 27 | 29 | 106 | 제공처 HTTP 429 / 코드 22 |
| 복지로 지자체 | 733 | 117 | 0 | 785 | 18페이지 응답 읽기 오류 |
| 합계 | 1,693 | 307 | 29 | 4,791 | 전체 범위 미완료 |

이 시점 전체 보관 정책은 **2,665건**, 최초 대비 **138건 순증**이다. 지자체는 중간 저장 오류 후 잠금 만료와 설정 복구를 확인하고 한 번 재개하여 정상 처리했지만, 이후 목록 응답 오류로 멈췄다. 중앙의 실패 29건은 제한 응답 후 상세 수집 실패로 기록됐으며 수집 성공이 아니다.

모든 수집 프로세스 종료, 잠금 해제, 세 출처의 작업·일일 한도 100 복구를 확인했다. 사용량은 그대로 보존했다. 정부24 DB 중단 사유는 마지막 배치의 `CALL_BUDGET`이며, 실행 스크립트의 종료 사유는 사용자 전환 요청에 따른 `STOP_REQUESTED`다.

증거: `.local/policy-sync/daily-20260909/continue-journal.json`, `local-recovery-journal.json`, `after-extension.json`. [추가 수집 JSON](../../fixtures/policy-automatic/collection-20260909-extension.json)에 누적 수치와 상세 증거 해시를 보관했다. 앞의 JSON 링크는 1차 실행 시점 기록이다.

<a id="policy-collection-20260909--미저장-우선-방식-전환"></a>

### 미저장 우선 방식 전환

이후 사용자 요청에 따라 새 CLI 실행의 기본값을 `new-only`로 변경했다. 기존 실행은 갱신 모드의 기록을 유지하고, 정부24 새 실행 `6cea7c91-158d-436a-85fc-950060c2aa52`를 생성했다. 기존 정책은 상세 호출 없이 건너뛰고 `skippedExisting`로 집계한다. [운영 계약](../work/collection-runbook.md)의 미저장 우선 수집 항목을 참조한다.

구현 검증: 수집기·실제 XML/DB 통합 테스트 21개, DB 회귀 테스트 25개 통과. 테스트 프로세스가 개별 테스트를 숨기는 환경이어서 `--test-isolation=none`으로 개별 통과를 확인했다. ESLint와 `tsc --noEmit`도 통과했다. 전체 빌드는 Turbopack의 포트 생성 제한으로 실패했고, webpack 대체 실행도 TypeScript `--showConfig` 출력 해석 오류로 완료하지 못했다.

개발 DB의 마이그레이션 `20260909091358` 적용과 신규 열 존재, RPC `SECURITY INVOKER`, `anon`/`authenticated` 실행 불가 및 `service_role` 실행 가능을 확인했다. Supabase 진단에는 이번 RPC 관련 경고가 없었고, 별도 기존 함수 `rls_auto_enable` 권한 경고 2개와 Auth 유출 비밀번호 보호 경고 1개가 남았다. 이 수집 변경에서 해당 설정을 수정하지 않았다. 웹 앱 배포는 수행하지 않았다.


<a id="policy-collection-20260909--오늘-수집-최종-중단"></a>

### 오늘 수집 최종 중단

사용자 요청에 따라 미저장 우선 수집을 중단했다. 종료 직전 저장 오류가 발생했으며, 프로세스 종료(exit 1), 한도 100 복구와 만료 잠금 해제를 확인했다. 추가 원천 호출 없이 남은 실행 상태를 `PARTIAL / USER_STOPPED`로 정리하고 직전 저장 오류를 summary에 보존했다. 루나의 감시도 종료했다.

전체 보관 정책은 **4632건**, 오늘 최초 2,527건 대비 **2105건 순증**이다. 전체 출처 수집 완료는 아니다. 미저장 우선 실행의 최종 집계: `{"calls": 12141, "failed": 0, "reason": "USER_STOPPED", "status": "PAUSED", "pending": 0, "success": 1967, "excluded": 2380, "precedingError": "automatic-storage-failed", "skippedExisting": 1803}`.

[최종 종료·복구 기록](../../fixtures/policy-automatic/collection-20260909-final.json). 다음 수집은 별도 실행 요청 전까지 재개하지 않는다.

## 2026-09-11 — 추천 구현과 병행한 신규 수집

사용자가 정책 수집을 별도 요청하여 기존 USER_STOPPED 실행의 재개 권한이 생겼다. Luna 실행을 요청했으나 세션 agent thread limit으로 생성이 거절되어 부모가 기존 CLI를 별도 프로세스로 실행했다. 기존 작업 에이전트는 추천 검증을 계속했다.

실행 전 DB 정책 4,632건, 당일 예약 호출 0, 활성 GOV24 수집 임대 없음을 확인했다. 이전 실행 `6cea7c91-158d-436a-85fc-950060c2aa52`는 목록 변경으로 1회 호출 뒤 SOURCE_DRIFT 종료했다. 새 new-only 실행 `6d2476ea-95c0-4875-8e26-b774f55561ef`를 시작하여 99회 호출했다. 기존 586건을 건너뛰고 무관 34건을 제외했다. 신규 반영은 0건이며 총 4,632건을 유지한다.

당일 내부 한도 100회에 도달해 PAUSED/CALL_BUDGET, 다음 페이지 14, 대기 30건을 보존했다. 프로세스 exit 0 및 비활성 임대 확인. 전체 수집은 미완료이며 이전 임시 상한을 복원하거나 새 한도로 높이지 않았다. 이후 이어서 수집하려면 실제 계정 잔여 할당량과 내부 일일 한도를 확인해야 한다.

[실행 요약 JSON](../../fixtures/policy-automatic/collection-20260911.json). 이번 수집 결과를 추천 규칙의 공개 승인으로 해석하지 않는다.

### 같은 날 재정제와 병행한 추가 수집 — 사용자 요청으로 종료

이후 사용자가 실제 API 한도에 도달할 때까지 추가 수집을 요청하고, **이번 정부24 실행에 한해 내부 상한 100,000회·최대 2시간 후 복구**를 명시적으로 허용했다. 이는 위 초기 100회 종료 이후의 별도 승인이다.

복지로 중앙 실행 `9216ed5b-2b00-474b-a9b5-f646d85cc414`는 100회 호출·신규 성공 73건, 지자체 실행 `430461c2-e846-43d6-ac8d-2e98962aa361`는 100회 호출·신규 성공 50건 후 내부 CALL_BUDGET으로 멈췄다. 두 출처의 상한은 100을 유지했으며 실제 API 할당량 소진으로 확정하지 않는다.

정부24 실행 `6d2476ea-95c0-4875-8e26-b774f55561ef`는 2026-09-11 22:59:28 KST에 계속 수집을 시작했다. 사용자가 오늘 수집을 멈추고 검토 대기 정제를 우선하도록 지시하여 23:55:06 KST에 SIGTERM을 보냈다. 진행 중 묶음을 마친 뒤 23:55:24 KST에 두 내부 한도를 100으로 복구했고, exit 0·프로세스 종료·비활성 임대를 확인했다. 관리자 실행 이유도 기존 집계를 보존한 채 `USER_STOPPED`로 정리했다.

최종 보관 정책 **5,895건**, 최초 4,632건 대비 신규 **1,263건**이다. 정부24 1,140건·복지로 중앙 73건·지자체 50건이다. 정부24 실행은 10,399회 호출, 무관 제외 3,347건, 기존 건너뜀 3,769건, 대기 44건으로 중단됐다. 앞선 SOURCE_DRIFT 1회를 포함한 당일 정부24 예약은 10,400회이며 사용량을 초기화하지 않았다. 실제 API 한도 소진이나 전체 출처 수집 완료는 아니다.

[최종 종료·복구 결과](../../fixtures/policy-automatic/collection-20260911-stopped.json). 비공개 전체 기록은 `.local/policy-sync/refinement-20260911/`에 보존한다. 관리자 REVIEW→ACTIVE 389건은 상태 전환이므로 신규 수집 수에 합산하지 않는다. 사용자 요청 전까지 수집을 재개하지 않는다.
