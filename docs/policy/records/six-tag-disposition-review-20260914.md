# 여섯 태그 전수 판정 검토 — 2026-09-14

## 기준과 범위

`policy-relevance-review-5` 적용 뒤 남은 REVIEW 1,406건의 전체 표시 원문을 여섯 화면 태그 기준으로 검토했다. 실제 대상과 지원 내용이 태그에 부합하면 ACTIVATE, 명확한 범위 밖이면 EXCLUDE, 원문 누락·충돌 때문에 관련성 자체를 판단할 수 없을 때만 KEEP_REVIEW로 판정했다. 연령·소득·거주·기간·신청방법 같은 세부 자격 확인은 보류 사유로 확대하지 않고 `conditionChecks`에 남겼다.

이번 작업은 저장된 Gov24 표시 원문을 기준으로 한 관련성 검토다. 모든 정책의 공식 사이트를 다시 수집하거나 최신 모집 여부와 개인별 수급 자격을 승인하지 않았다.

## 검토 결과

469건, 469건, 468건의 세 레인으로 나누어 1차 판정한 뒤, 각 레인의 활성·비활성 판정을 독립 교차검토했다. 최종 결정은 1,406건과 일대일로 대응하며 누락과 중복은 없다.

| 결정 | 건수 | 적용 시 상태 |
| --- | ---: | --- |
| ACTIVATE | 963 | ACTIVE |
| EXCLUDE | 364 | EXCLUDED |
| KEEP_REVIEW | 79 | REVIEW 유지 |

ACTIVATE의 태그 지정 건수는 중복 태그를 포함해 주거·생활지원 653건, 의료·건강 334건, 돌봄 94건, 아동 교육 4건이다. KEEP_REVIEW에는 학교급이 없는 장학금, 실제 급여가 빠진 사회서비스·안전보험, 품목이 없는 위문품처럼 표시 원문만으로 태그 관련성을 확정할 수 없는 항목만 남겼다.

## 구현과 검증

- `policy-relevance-review-6`은 ACTIVATE·EXCLUDE·KEEP_REVIEW를 모두 지원한다. ACTIVATE에는 정확한 `target_text`와 `benefit_text`, EXCLUDE에는 대상·지원·선정기준 중 하나 이상의 직접 근거가 필요하다.
- 원문·스냅샷·직전 판정을 계획에 고정하고, 동일 요청 재시도와 이전 버전의 덮어쓰기 방지를 유지했다. 공개 추천과 관리자 조회도 v6 활성 판정을 인식한다.
- 사전 계획은 1,406건 전부를 포함하며 ACTIVATE 963건, EXCLUDE 364건, KEEP_REVIEW 79건, 미검토 0건이다. 계획 digest는 `c90ab2dc32337bcdbb563e9bde44b5444b0d715edce3c3db003fc077c0c911cd`다.
- 관련 단위·DB 통합 테스트 58건, 변경 파일 ESLint, diff 공백 검사가 통과했다.

## 적용 상태

명시적 승인 뒤 `20260913162532_policy_six_tag_disposition` 마이그레이션과 고정된 전체 계획을 원격 DB에 적용했다. 1,406건 모두 저장 직후 재조회 검증을 통과했고, 독립 검증에서 v6 관찰 이력 1,406건과 공개 활성 조회 4,414건을 확인했다.

| 상태 | 이전 | 이후 |
| --- | ---: | ---: |
| ACTIVE | 3,451 | 4,414 |
| REVIEW | 1,406 | 79 |
| EXCLUDED | 1,038 | 1,402 |

함수는 `security invoker`이고 `public`, `anon`, `authenticated`에는 실행 권한이 없다. 적용 후 Supabase 어드바이저는 작업 전과 동일한 기존 경고 3건만 보고했으며 이번 변경에서 새 경고는 발생하지 않았다. 기존 경고는 `rls_auto_enable()`의 익명·인증 사용자 실행 권한 2건과 유출 비밀번호 보호 비활성 1건으로, 이번 범위에서는 변경하지 않았다.

전체 판정과 직접 원문 근거, 조건 확인 항목, 계획·적용 digest는 [검토 기록 JSON](../../fixtures/policy-recommendation/six-tag-disposition-review-20260914.json)에 보존했다. 적용 당시 실행 계획에는 스냅샷 ID, 표시 해시, 직전 판정과 정규화 원문이 포함됐지만 중복 원문이 큰 실행용 plan 자체는 저장소에 보존하지 않았다. 따라서 compact 기록만으로 계획 digest를 다시 계산할 수는 없으며, 원격 관찰 이력과 현재 정책 재조회로 적용 결과를 검증한다.
