# Server only. Use the approved public-data portal key; never commit actual values.
GOV24_API_KEY=

# Required later for DB ingestion; MCP authentication does not populate these.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Enable only after manual validation and deployment configuration.
POLICY_SYNC_ENABLED=
POLICY_SYNC_CRON_SECRET=
